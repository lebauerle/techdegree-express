extends layout.pug

block content 
  article.grid-container.portfolio-intro
    div

      h1 Oh Hello there, welcome to my portfolio!

      p.lead.text-light
        | Go check out my portfolio. Most of these projects are from the Team Treehouse Techdegree, but soon my own projects will be added!
  article.grid-container.portfolio-index
    
    .grid-x.grid-margin-x.small-up-2.medium-up-2.large-up-3


      each project in projects
        .cell
          a(href=`/project/${project.id}`)
            img.thumbnail(src=`./static/images/thumbnail_project_${project.id + 1}.png`)
            h5= project.project_name
      
